import { Router } from "express";

const router = Router();
router.post(
  "/create-disease",

  /*  #swagger.tags = ['5.Disease']
      #swagger.summary = 'Create Disease'
      #swagger.description = 'Create Disease'

      #swagger.parameters['body'] = {
        in: 'body',
        required: true,
        schema: {
          
        }
      }

      #swagger.responses[200] = {
        description: 'Guest Created',
        schema: {
          success: 1,
          data: {
            user_id: 'TEMP_1750000000000'
          },
          message: 'Guest Created'
        }
      }
  */
);
